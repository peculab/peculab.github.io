rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Field Notes currently uses share-only reader actions.
    // Keep the archived database closed unless a future feature needs it.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
