// Fill these two public values only after the private Worker and Turnstile are deployed.
// Empty values keep the existing FormSubmit email form and reviewed JSON snapshot working.
window.BRIDGE_BACKEND = { url: "", turnstileSiteKey: "" };
